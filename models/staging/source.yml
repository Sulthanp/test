version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('db_dev')}}"
      schema: raw_schema
      tables:
        - name: raw_customersdata
        - name: orders
        - name: customers
        - name: retro
          columns:
            - name: id
              tests:
              - unique
              - not_null
            
